#include "main.h"
/**
 * print_rev - reverse string
 * @s: First operand
 *
 */
void print_rev(char *s)
{
	len = _strlen(s);
	char *start = (s + (len - 1));
	while (*tart)
		_putchar(*str++);

	_putchar('\n');
}
